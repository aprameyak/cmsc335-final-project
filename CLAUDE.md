# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

CMSC335 final project. Topic and frontend framework are TBD — confirm with the user before scaffolding.

## Requirements (all must be satisfied)

- Node.js + Express.js backend (no PHP)
- `express.Router()` for at least some endpoints
- Mongoose to interact with MongoDB
- Store and retrieve data from MongoDB
- At least one HTML form where users submit data
- CSS file using `background-color`, `color`, `font-size`, and a Google Font
- At least one external API providing data (weather, images, etc.)
- Any libraries/frameworks are allowed (React, Vite, etc.)

Before calling the project complete, verify all 8 requirements above are covered.
